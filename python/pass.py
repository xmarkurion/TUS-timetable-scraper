import sys
import getopt
#
# def mainFunc(argv):
#     arg_course_name = ""
#     arg_help = "{0} -i <input> -u <user> -o <output>".format(argv[0])
time_table = sys.argv[1]
print("Cmd entries: ", sys.argv)
print(f'Generating table for the: {time_table}')
