import { test } from '@playwright/test';
import { Courses } from '../pages/courses.page';

test('Checking if i can run this', async ({ page, context }) => {
    console.log("If you see this all is good!");
  });
